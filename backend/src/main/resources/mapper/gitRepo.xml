<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.GitRepoDao">

	<!-- 저장소 연결 -->
	<insert id="insert" parameterType="gitRepo">
		<selectKey keyProperty="repoId" resultType="int" order="BEFORE">
			SELECT nextval('flowtask_git_repo_seq')
		</selectKey>
		INSERT INTO flowtask_git_repo (repo_id, team_id, provider, repo_owner, repo_name, access_token, created_at)
		VALUES (#{repoId}, #{teamId}, #{provider}, #{repoOwner}, #{repoName}, #{accessToken}, CURRENT_TIMESTAMP)
	</insert>

	<!-- 팀별 저장소 조회 -->
	<select id="getByTeamId" parameterType="int" resultType="gitRepo">
		SELECT repo_id, team_id, provider, repo_owner, repo_name, access_token, created_at
		FROM flowtask_git_repo
		WHERE team_id = #{teamId}
	</select>

	<!-- ID로 조회 -->
	<select id="getById" parameterType="int" resultType="gitRepo">
		SELECT repo_id, team_id, provider, repo_owner, repo_name, access_token, created_at
		FROM flowtask_git_repo
		WHERE repo_id = #{repoId}
	</select>

	<!-- 저장소 수정 -->
	<update id="update" parameterType="gitRepo">
		UPDATE flowtask_git_repo
		SET provider = #{provider},
			repo_owner = #{repoOwner},
			repo_name = #{repoName},
			access_token = #{accessToken}
		WHERE repo_id = #{repoId}
	</update>

	<!-- 저장소 연결 해제 -->
	<delete id="delete" parameterType="int">
		DELETE FROM flowtask_git_repo WHERE repo_id = #{repoId}
	</delete>

</mapper>
