<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.MemberDao">

	<!-- 회원가입 -->
	<insert id="insert" parameterType="member">
		INSERT INTO kari_member (no, userid, password, name, email, phone, register)
		VALUES (kari_member_seq.nextval, #{userid}, #{password}, #{name}, #{email}, #{phone}, sysdate)
	</insert>

	<!-- 아이디 중복 체크 -->
	<select id="checkUserid" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM kari_member WHERE userid = #{userid}
	</select>

	<!-- 이메일 중복 체크 -->
	<select id="checkEmail" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM kari_member WHERE email = #{email}
	</select>

	<!-- 로그인 (기존 - 더 이상 사용 안함) -->
	<select id="login" parameterType="member" resultType="member">
		SELECT * FROM kari_member WHERE userid = #{userid} AND password = #{password}
	</select>

	<!-- userid로 회원 조회 (BCrypt 비밀번호 검증용) -->
	<select id="findByUserid" parameterType="String" resultType="member">
		SELECT * FROM kari_member WHERE userid = #{userid}
	</select>

	<!-- 아이디 찾기 -->
	<select id="findUserid" parameterType="member" resultType="String">
		SELECT userid FROM kari_member WHERE name = #{name} AND email = #{email}
	</select>

	<!-- 비밀번호 찾기 전 회원 확인 -->
	<select id="findMemberForPassword" parameterType="member" resultType="member">
		SELECT * FROM kari_member WHERE userid = #{userid} AND email = #{email}
	</select>

	<!-- 비밀번호 변경 -->
	<update id="updatePassword" parameterType="member">
		UPDATE kari_member SET password = #{password} WHERE no = #{no}
	</update>

</mapper>
