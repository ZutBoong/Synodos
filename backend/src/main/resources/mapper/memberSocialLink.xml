<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.MemberSocialLinkDao">

	<!-- 소셜 연동 추가 -->
	<insert id="insert" parameterType="memberSocialLink">
		INSERT INTO member_social_link (member_no, provider, provider_id, email, name, linked_at)
		VALUES (#{memberNo}, #{provider}, #{providerId}, #{email}, #{name}, CURRENT_TIMESTAMP)
	</insert>

	<!-- 소셜 연동 해제 -->
	<delete id="delete">
		DELETE FROM member_social_link
		WHERE member_no = #{memberNo} AND provider = #{provider}
	</delete>

	<!-- 회원의 모든 소셜 연동 조회 -->
	<select id="findByMemberNo" parameterType="int" resultType="memberSocialLink">
		SELECT * FROM member_social_link
		WHERE member_no = #{memberNo}
		ORDER BY linked_at DESC
	</select>

	<!-- provider + provider_id로 연동 조회 -->
	<select id="findByProviderAndProviderId" resultType="memberSocialLink">
		SELECT * FROM member_social_link
		WHERE provider = #{provider} AND provider_id = #{providerId}
	</select>

	<!-- 회원의 특정 provider 연동 여부 확인 -->
	<select id="findByMemberNoAndProvider" resultType="memberSocialLink">
		SELECT * FROM member_social_link
		WHERE member_no = #{memberNo} AND provider = #{provider}
	</select>

</mapper>
